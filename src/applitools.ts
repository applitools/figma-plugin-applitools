
//import * as _ from "@applitools@eyes-images'";
//const _: any = require('@applitools/eyes-images');
//let eyes = require("eyes.images");
//import Eyes from 'eyes.images'
//var x = require("eyes.images")
import {MatchLevel, Eyes} from "@applitools/eyes-images";
//import * as fetch from "node-fetch";
export async function test(): Promise<void> { 
  //console.log('Test!');
  //debugger;
  //new Eyes();
  figma.closePlugin();

  // Initialize thcxcce eyes configuration
  //const configuration = new Configuration();

  // You can get your api key from the Applitools dashboard
  // configuration.setApiKey('APPLITOOLS_API_KEY')

  // Set new batch
  //configuration.setBatch(new BatchInfo('Demo batch'))

  // Set the configuration to eyes
  //eyes.setConfiguration(configuration);

  try {
    //await eyes.open('Applitools site', 'Screenshot test!', {width: 800, height: 600})

    //await eyes.checkImage('https://i.ibb.co/bJgzfb3/applitools.png')
    
    //const imageBuffer = await fetch('https://i.ibb.co/bJgzfb3/applitools.png').then(resp => resp.arrayBuffer())
    //await eyes.check('Buffer', Target.image(imageBuffer))

    //await eyes.close()
  }
  catch (e) {
    //await eyes.abortIfNotClosed();
  }

} 