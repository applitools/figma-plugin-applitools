export async function readFile(filePath, _options, callback) {
  const directory = filePath.includes('dom-snapshot') ? 'dom-snapshot' : 'dom-capture'
}
export const constants = {O_CREAT: true}
export async function writeFile() {
  return null
}